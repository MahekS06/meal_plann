import React from "react";
import {
    View,
    Text,
    TouchableOpacity,
    Image,
    StyleSheet,
    Button,
  } from "react-native";

  const NutritionalScreen = ({ navigation }) => {
    return (
        <View>
            <Text>Sign in with account</Text>
        </View>

            );
        };
