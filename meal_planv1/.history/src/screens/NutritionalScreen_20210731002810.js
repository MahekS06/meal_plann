import React from "react";
import {
    View,
    Text,
    TouchableOpacity,
    Image,
    StyleSheet,
    Button,
  } from "react-native";

  const LandingScreen = ({ navigation }) => {
    return (
        <View>
            <Text>Sign in with account</Text>
        </View>

            );
        };
