import React from "react";
import {
    View,
    Text
  } from "react-native";

  const NutritionalScreen = ({ navigation }) => {
    return (
        <View>
            <Text>Sign in with account</Text>
        </View>

            );
        }
